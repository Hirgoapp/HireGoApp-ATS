apiVersion: 1

# List of dashboard providers
providers:
  - name: 'ATS Dashboards'
    orgId: 1
    folder: 'ATS'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
